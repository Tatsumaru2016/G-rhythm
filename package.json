{
  "name": "g-rhythm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/strip-deploy-models.mjs",
    "build:check": "tsc && vite build",
    "sync:charts": "tsx scripts/sync-builtin-charts.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "audio-decode": "^3.11.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "three": "^0.185.0"
  }
}
